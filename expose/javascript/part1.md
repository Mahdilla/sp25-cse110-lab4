1. values added: 20
2. final result: 20
3. We should not use var because it has no block scope. Instead we should use let and const to declare variables.
4. values added: 20
5. This code returns a ReferenceError because result is not defined. This is because we used let.
6. This line returns an error because once we define a variable with const its value cannot be changed. 
7. This line does not print anything as the code is never reached due to the prior error.
